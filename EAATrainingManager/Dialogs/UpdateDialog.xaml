<?xml version="1.0" encoding="utf-8" ?>
<ContentDialog
    x:Class="EAATrainingManager.Dialogs.UpdateDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="تحديثات المنظومة الذكية (تحديث تفاضلي خفيف)"
    CloseButtonText="إغلاق">

    <StackPanel Spacing="16" Padding="6" MinWidth="450">
        <!-- Current Version Badge -->
        <Border Style="{StaticResource FluentCardBorder}" Background="{ThemeResource LayerFillColorAltBrush}" Padding="14">
            <Grid ColumnSpacing="12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <FontIcon Grid.Column="0" Glyph="&#xE895;" FontSize="28" Foreground="#0D6EFD" FlowDirection="LeftToRight" VerticalAlignment="Center" />
                <StackPanel Grid.Column="1" Spacing="4">
                    <TextBlock Text="الأكاديمية المصرية لعلوم الطيران — نظام إدارة التدريب" FontWeight="Bold" FontSize="13" />
                    <TextBlock x:Name="TxtCurrentVersion" Text="الإصدار المثبت: v2.2.0 (نسخة رسمية مستقلة)" FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                </StackPanel>
            </Grid>
        </Border>

        <!-- Low-Bandwidth Delta Explanation -->
        <Border Background="{ThemeResource LayerFillColorDefaultBrush}" CornerRadius="6" Padding="12">
            <StackPanel Spacing="4">
                <TextBlock Text="تقنية التحديث الخفيف لشبكات الطيران الضعيفة:" FontSize="12" FontWeight="Bold" />
                <TextBlock Text="تعتمد المنظومة تقنية التحديثات التفاضلية (Delta Patches) بحجم 2–5 ميجابايت فقط بدلاً من إعادة تحميل حزمة البرنامج الكاملة (~240 ميجابايت)، مما يتيح التحديث السلس عبر شبكات المحمول الضعيفة." FontSize="11" Foreground="{ThemeResource TextFillColorSecondaryBrush}" TextWrapping="Wrap" />
            </StackPanel>
        </Border>

        <!-- Progress Indicator -->
        <ProgressBar x:Name="CheckProgressBar" IsIndeterminate="True" Height="4" Visibility="Collapsed" />

        <!-- Status Message -->
        <TextBlock x:Name="TxtUpdateStatus" Text="اضغط على زر الفحص للتحقق من وجود تحديثات جديدة." FontSize="12" Foreground="{ThemeResource TextFillColorPrimaryBrush}" TextWrapping="Wrap" />

        <!-- Check Button -->
        <Button x:Name="BtnCheckNow" Click="BtnCheckNow_Click" Style="{StaticResource AccentButtonStyle}" HorizontalAlignment="Stretch" Padding="12,8">
            <StackPanel Orientation="Horizontal" Spacing="8" HorizontalAlignment="Center">
                <FontIcon Glyph="&#xE72C;" FontSize="13" FlowDirection="LeftToRight" />
                <TextBlock Text="فحص التحديثات المتاحة الآن" FontWeight="SemiBold" />
            </StackPanel>
        </Button>
    </StackPanel>
</ContentDialog>
